// Checkbox Group accepts arbitrary GPUI children because Base UI examples place
// checkboxes inside labels, Field items, and nested Checkbox Groups. Context is
// injected by the root scope element rather than a typed child traversal.
